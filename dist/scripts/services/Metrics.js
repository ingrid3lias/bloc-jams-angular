(function(){
    function Metrics($rootScope){
        var Metrics = {},
            
        return Metrics;
    }
    
    angular
        .module('blocJams')
        .factory('Metrics', ['$rootScope', Metrics]);
    
})();